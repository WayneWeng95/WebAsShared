NodeAgent/target/
Executor/target/
node-agent
Executor/.claude/
.claude/
Partitioner/target/
TestData/*
TestOutput/
# …but DO track the tiny (12 KiB) WordCount seed so it can't be lost again on a
# cluster migration — every corpus_* tiles from it (Scripts/gen_testdata.sh).
!TestData/corpus.txt
Tests/Micro-Benchmarks/StateSync-local/.run
Tests/Micro-Benchmarks/StateSync-remote/.run

# Build artifacts
__pycache__/
*.pyc

# Regenerable test corpora — create via scripts/gen_corpora.sh.
# Only the small corpus.txt seed is tracked.
Executor/data/corpus_large.txt

*.tar